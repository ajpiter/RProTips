#R will automatically convert the datatype when you add two different datatypes togehter 

#if you add, the datatype will be converted to  
logical + integer = interger 
logical + numeric = numeric 
logical + character = character 
interger + numeric = numeric 
interger + character = character 
numeric + character = character 
